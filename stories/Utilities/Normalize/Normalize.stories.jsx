import { Normalize } from "./Normalize";

export default {
  title: "Components/Normalize",
  component: Normalize,
};

export const DefaultNormalize = {
  render: () => <Normalize />,
  name: "Normalize",
};
