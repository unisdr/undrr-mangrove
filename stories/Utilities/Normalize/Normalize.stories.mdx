import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Normalize } from './Normalize';

<Meta title="Components/Normalize" component={Normalize} />

# Normalize

Lighter than a CSS reset, this helps smooth inconsistencies between browsers.

<Canvas>
  <Story name="Normalize">
    <Normalize></Normalize>;
  </Story>
</Canvas>

### Usage
To use this component, follow these steps:
- Include the CSS

#### Interactions
- No interactions are needed with the loader

### Changelog
1.0 — Released component
