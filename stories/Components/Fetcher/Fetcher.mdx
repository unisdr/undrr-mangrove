import { Meta, Story, Canvas, Source } from '@storybook/addon-docs/blocks';
import Fetcher from './Fetcher';
import * as FetcherStories from './Fetcher.stories';

<Meta of={FetcherStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Fetcher

> **⚠ Deprecated.** This component is deprecated as of v1.7.x and will be removed in **v1.8**. The render-prop fetching pattern has been superseded by ecosystem libraries with caching, retries, and suspense integration. New code should use [react-query](https://tanstack.com/query), [SWR](https://swr.vercel.app/), or server-side data loading. See the [removal tracker](https://github.com/unisdr/undrr-mangrove/issues/1011) for the migration plan.

The `Fetcher` component is a flexible data-fetching utility for React applications.

## Props

- **`api`**: `string` - The URL of the API endpoint. _(required)_
- **`render`**: `function` - A function to render the UI based on the fetched data. _(required)_
- **`queryParams`**: `object` - Additional query parameters to send with the API request. _(optional)_
- **`loadingLabel`**: `string` - A label to display while loading. _(optional)_

## Stories

### Default

This is the default usage of the `Fetcher` component.

<Canvas of={FetcherStories.Default} meta={FetcherStories} />

<Source
  language="jsx"
  code={`
import Fetcher from './Fetcher';

const Default = () => {
const renderData = ({ isLoading, data }) => (

<div>
  {isLoading && <p>Loading data...</p>}
  <pre>{JSON.stringify(data, null, 4)}</pre>
</div>
);

return (

<Fetcher
  api="https://sfvc-migration.undrr.org/api/v2/content/commitments-metrics"
  queryParams={{ items_per_page: 1000 }}
  loadingLabel="Loading data"
  render={renderData}
/>
); };

export default Default;
`}
/>

### With Search Term

An example of the `Fetcher` component with a search term parameter.

<Canvas of={FetcherStories.WithSearchTerm} meta={FetcherStories} />

<Source
  language="jsx"
  code={`
import Fetcher from './Fetcher';

const WithSearchTerm = () => {
const renderData = ({ isLoading, data }) => (

<div>
  {isLoading && <p>Searching...</p>}
  <pre>{JSON.stringify(data, null, 4)}</pre>
</div>
);

return (

<Fetcher
  api="https://sfvc-migration.undrr.org/api/v2/content/commitments-metrics"
  queryParams={{ items_per_page: 1000 }}
  loadingLabel="Searching"
  render={renderData}
/>
); };

export default WithSearchTerm;
`}
/>

### Without Query Params

This example shows how to use the `Fetcher` component without passing any query parameters.

<Canvas of={FetcherStories.WithoutQueryParams} meta={FetcherStories} />

<Source
  language="jsx"
  code={`
import Fetcher from './Fetcher';

const WithoutQueryParams = () => {
const renderData = ({ isLoading, data }) => (

<div>
  {isLoading && <p>Loading data...</p>}
  <pre>{JSON.stringify(data, null, 4)}</pre>
</div>
);

return (

<Fetcher
  api="https://sfvc-migration.undrr.org/api/v2/content/commitments-metrics"
  loadingLabel="Loading data"
  render={renderData}
/>
); };

export default WithoutQueryParams;
`}
/>

### Accessibility

Ensure that the `render` function used within `Fetcher` is accessible and handles data presentation properly for all users.

## Changelog

- **1.1** — 2026-05-12: Deprecated. Will be removed in v1.8; new code should use [react-query](https://tanstack.com/query), [SWR](https://swr.vercel.app/), or server-side data loading. See the [removal tracker](https://github.com/unisdr/undrr-mangrove/issues/1011).
- **1.0** — 2024-09-04: Initial release
