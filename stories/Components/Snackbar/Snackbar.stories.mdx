
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { CtaButton } from './../Buttons/CtaButton/CtaButton';

import {ShowOffSnackbar} from './Snackbar.jsx';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: 'Read more'}; return engText;
    case 'arabic': const arabicText = {detail: 'اقرأ أكثر'}; return arabicText;
    case 'burmese': const burmeseText = {detail: 'ပိုပြီးဖတ်ပါ'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: '続きを読む'}; return japaneseText;
    default:
      return {detail: 'Read more'};
  }
};


 

<Meta title="Components/Snackbar"
  argTypes={{
    message: {
      control: { type: "text" },
      defaultValue: 'Message ...'
      },
    severity: {
      options: ["Active", "Disabled"],
      control: { type: "inline-radio" },
      defaultValue: 'Active'
      },
      onClose: {
      options: ["Arrow", "No Arrow"],
      control: { type: "inline-radio" },
      defaultValue: 'Arrow'
      },
  }}/>

## How to use the snackbar component:
* add react state to the component that will use the snackbar(example App.js)
* change the message, severity, onClose props to the values you want to use
* controll, whether it is visible or not with the state you added to the component and connecting it to the oppened prop

<Canvas>
<Story name="Snackbar">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <ShowOffSnackbar/>;
 
  }}
</Story>
</Canvas>
