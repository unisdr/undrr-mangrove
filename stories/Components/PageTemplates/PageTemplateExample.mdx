import { Meta, Story, Canvas } from '@storybook/addon-docs';
import * as PageTemplateExampleStories from './PageTemplateExample.stories.js';

<Meta of={PageTemplateExampleStories} title="Components/Page Template Example" />

# Page Template Example

This is the `PageTemplateExample` component, which demonstrates how severla components can be pulled together on a single page. This is currently in a proof-of-concept phase and will be expanded, along with addtional variants (news article, event page, landing page and so on).

Last updated on: October 2024.

<Story of={PageTemplateExampleStories.Default} />
