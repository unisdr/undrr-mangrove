import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Chips } from './Chips';
import * as ChipsStories from './Chips.stories';

export const getCaptionForLocale = locale => {
  switch (locale) {
    case 'english':
      const engText = { detail: 'Label' };
      return engText;
    case 'arabic':
      const arabicText = { detail: 'ملصق' };
      return arabicText;
    case 'japanese':
      const japaneseText = { detail: 'ラベル' };
      return japaneseText;
    default:
      return { detail: 'Label' };
  }
};

<Meta of={ChipsStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# Chips

Small interactive tags used to show active filters, selections, or categories. Chips can include a dismiss icon to let users remove them.

## When to use

- Showing which filters or search terms are currently active
- Letting users remove individual selections without clearing everything
- Labelling active categories or tags in a content listing

## States

**Default (with dismiss icon)** — The chip shows a label and a close icon. Clicking the icon removes the filter or selection.

**Hover (with dismiss icon)** — The chip changes visual appearance when the user hovers over it, signalling it is interactive.

**Default (no dismiss icon)** — The chip shows a label only. It communicates a current selection or category but does not allow removal.

**Hover (no dismiss icon)** — Same visual change as the hover state above; no dismiss action is available.

<Canvas of={ChipsStories.DefaultChips} meta={ChipsStories} />

## Usage

### Chip with dismiss

```html
<span class="mg-chip">
  Label
  <button class="mg-chip__cross" aria-label="Remove Label filter">×</button>
</span>
```

### Chip without dismiss

```html
<span class="mg-chip">Label</span>
```

Copy from the HTML tab in the canvas above for the full rendered output with any additional wrapper classes.

## Interactions

- The hover state applies to the entire chip.

## Accessibility

- The dismiss button is icon-only. Provide an accessible name via `aria-label` that includes the filter value: `aria-label="Remove [label text]"` — for example, `aria-label="Remove Sendai Framework"`.
- Keyboard users activate the dismiss button with **Enter** or **Space**.
- After a chip is dismissed, move focus to the next chip or the related search input — do not drop focus.

## Changelog

- **1.1** — 2026-03-21: Renamed CSS classes to `mg-` prefix (`chip` → `mg-chip`, `chip__cross` → `mg-chip__cross`).
- **1.0** — 2022-12-12: Released component
