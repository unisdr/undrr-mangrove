import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import * as SyndicationSearchWidgetStories from './SyndicationSearchWidget.stories';

<Meta of={SyndicationSearchWidgetStories} />

> If you are creating or modifying this component, see [docs/REVIEW-CHECKLIST.md](../../../docs/REVIEW-CHECKLIST.md) for Mangrove's component standards.

# UNDRR syndication search widget

The **UNDRR syndication search widget** lets users search and filter content across UNDRR domains — undrr.org, preventionweb.net, and related sites. It provides keyword search, faceted filtering, and multiple display modes (list, card, or book card) in a single embeddable component.

> **Platform dependency:** This component requires access to the UNDRR search backend via `/search-endpoint`. It includes domain configurations, content type mappings, and taxonomy endpoints specific to the UNDRR platform. It is **not** a general-purpose search component.

> **Future:** A standalone, configurable search component without platform dependencies may be developed separately for broader use cases.

## Features

- Keyword search with responsive input — results update as you type
- Non-blocking result updates keep the page interactive while results load
- Removable filter chips for active filters
- Full ARIA support, keyboard navigation, screen reader announcements
- Styled with Mangrove design tokens
- Left-slide filter drawer on mobile, inline sidebar on desktop
- Custom select dropdowns (searchable, multi-select) with no external dependencies like Chosen.js

> **Implementation note (for contributors):** The non-blocking search experience is powered by React 19's concurrent rendering features: `useDeferredValue` keeps the search input responsive while results load in the background; `useTransition` marks filter changes as non-urgent to prevent UI blocking.

## Default

A basic search widget with default configuration.

<Canvas of={SyndicationSearchWidgetStories.Default} meta={SyndicationSearchWidgetStories} />

## Default query

A search widget initialized with a pre-filled search query.

<Canvas of={SyndicationSearchWidgetStories.DefaultQuery} meta={SyndicationSearchWidgetStories} />

## Hidden facets

A search widget with the facets sidebar hidden.

<Canvas of={SyndicationSearchWidgetStories.HiddenFacets} meta={SyndicationSearchWidgetStories} />

## Minimal configuration

Search-only mode without result counts, timers, or filters.

<Canvas of={SyndicationSearchWidgetStories.Minimal} meta={SyndicationSearchWidgetStories} />

## Debug metrics

Shows search performance metrics (score, interestingness, longevity) for debugging.

<Canvas of={SyndicationSearchWidgetStories.DebugMetrics} meta={SyndicationSearchWidgetStories} />

## Card display

Renders search results as vertical cards in a responsive grid. `gridColumns` controls the grid column count (2-6); if not set, it defaults to `resultsPerPage`. Drupal image styles are automatically rewritten to `landscape_16_9` (16:9) to match the vertical card aspect ratio.

<Canvas of={SyndicationSearchWidgetStories.CardDisplay} meta={SyndicationSearchWidgetStories} />

## Card book display

Renders search results as vertical book cards in a grid. Uses `mg-card__vc mg-card__book` classes. Drupal image styles are automatically rewritten to `por` (3:4 portrait) to match the book card aspect ratio.

<Canvas of={SyndicationSearchWidgetStories.CardBookDisplay} meta={SyndicationSearchWidgetStories} />

## Hidden teaser fields

Hide specific fields from pre-rendered teaser HTML using `visibleTeaserFields`. Set a field key to `false` to hide it; `null` (default) shows all fields. Uses CSS modifier classes for zero JS overhead.

<Canvas of={SyndicationSearchWidgetStories.HiddenTeaserFields} meta={SyndicationSearchWidgetStories} />

**Toggleable fields:** `image`, `contentType`, `publicationType`, `date`, `siteName`, `summary`, `organization`. Title is intentionally not toggleable.

## Custom filters

`customFilters` are raw Elasticsearch query_string filters applied invisibly to all searches.
Users cannot see or remove these filters - they constrain the search scope.

**This example uses:**
```js
customFilters: [
  'type:(news OR publication)',      // Only news and publications
  'published_at:[2023-01-01 TO *]',  // Content from 2023 onwards
]
```

<Canvas of={SyndicationSearchWidgetStories.CustomFilters} meta={SyndicationSearchWidgetStories} />

**Common filter patterns:**
- `type:news` - Restrict to content type
- `type:(news OR publication)` - Multiple types (OR)
- `published_at:[2023-01-01 TO *]` - Date range
- `field_domain_access:www_preventionweb_net` - Domain filter
- `field_themes:123` - Taxonomy filter

## Custom facets

`customFacets` create user-selectable dropdown filters in the sidebar.
Each facet maps labels to Elasticsearch queries.

**This example uses:**
```js
customFacets: [
  {
    id: 'region',
    title: 'Region',
    multiSelect: false,
    options: [
      { label: 'Africa', query: 'field_regions:1' },
      { label: 'Americas', query: 'field_regions:2' },
      // ...
    ],
  },
  {
    id: 'hazard_type',
    title: 'Hazard Type',
    multiSelect: true,  // Multiple selections allowed
    options: [
      { label: 'Earthquake', query: 'field_hazards:101' },
      { label: 'Flood', query: 'field_hazards:102' },
      // ...
    ],
  },
]
```

<Canvas of={SyndicationSearchWidgetStories.CustomFacets} meta={SyndicationSearchWidgetStories} />

**Structure:**
```json
{
  "id": "unique_id",
  "title": "Display Title",
  "weight": 10,
  "multiSelect": false,
  "options": [
    { "label": "Option Label", "query": "field_name:value" }
  ]
}
```

| Property | Type | Description |
|----------|------|-------------|
| `id` | string | Unique identifier for the facet |
| `title` | string | Label shown in the sidebar |
| `weight` | number | Sort order (lower = higher) |
| `multiSelect` | boolean | Allow multiple selections |
| `options` | array | Array of `{label, query}` pairs |

## Custom filters and facets

Use both together: `customFilters` for hidden constraints, `customFacets` for user choices.

**This example uses:**
```js
// Hidden constraint - users only see publications
customFilters: ['type:publication'],

// User-selectable options within publications
customFacets: [
  {
    id: 'publication_year',
    title: 'Publication Year',
    options: [
      { label: '2024', query: 'published_at:[2024-01-01 TO 2024-12-31]' },
      { label: '2023', query: 'published_at:[2023-01-01 TO 2023-12-31]' },
      // ...
    ],
  },
  {
    id: 'document_type',
    title: 'Document Type',
    options: [
      { label: 'Policy Brief', query: 'field_publication_type:601' },
      { label: 'Technical Report', query: 'field_publication_type:602' },
      // ...
    ],
  },
]
```

<Canvas of={SyndicationSearchWidgetStories.CustomFiltersAndFacets} meta={SyndicationSearchWidgetStories} />

## Allowed types

Restricts which content types appear in the **Type** dropdown filter. Unlike `customFilters`, this doesn't hide content—it limits the filter UI options.

**This example uses:**
```js
allowedTypes: {
  types: ['news', 'publication', 'event'],  // Only these in Type dropdown
  newsTypes: ['751', '752'],                // Only Update & Press release subtypes
}
```

<Canvas of={SyndicationSearchWidgetStories.AllowedTypes} meta={SyndicationSearchWidgetStories} />

**Available type IDs:** `news`, `event`, `publication`, `landing`, `vacancy`, `resource`, `collections`, `blog`, `terminology`, `organization`, `national_platform`

**News subtype IDs:** `751` (Update), `752` (Press release), `1` (Statements), `754` (Feature), `797` (Community announcement), `756` (Op Ed)

## Taxonomy term results

Demonstrates mixed search results containing both content nodes and taxonomy terms (Hazard, Country, Theme). Taxonomy terms display differently: vocabulary name as type badge, no date, defaults to preventionweb.net.

<Canvas of={SyndicationSearchWidgetStories.TaxonomyTermResults} meta={SyndicationSearchWidgetStories} />

## Custom endpoint

Override `searchEndpoint` to point at a different Elasticsearch proxy (DDEV, staging, etc.).

<Canvas of={SyndicationSearchWidgetStories.CustomEndpoint} meta={SyndicationSearchWidgetStories} />

## Syndicated cards

A hardwired topic query rendered as a content block with no search chrome. Uses `defaultQuery` to seed a topic, `requireImage` to ensure every card has a visual, and `visibleTeaserFields` to strip the content type badge and teaser text for a clean image + title + date card. Adjust `resultsPerPage` and `gridColumns` for different card counts.

<Canvas of={SyndicationSearchWidgetStories.SyndicatedCards} meta={SyndicationSearchWidgetStories} />

## Query syntax

Use this story to test search queries and explore the supported syntax, which includes:

- Basic queries and phrase matching
- Wildcards and fuzzy search
- Boolean operators (AND, OR, NOT)
- Field-specific searches
- Date ranges and boosting

<Canvas of={SyndicationSearchWidgetStories.QuerySyntax} meta={SyndicationSearchWidgetStories} />

See the story's Docs tab for a comprehensive syntax reference.

> **Implementation note (for contributors):** The search input uses Elasticsearch `query_string` syntax, forwarded directly to the `/search-endpoint` proxy.

## Static mockup

A static HTML mockup for CSS testing without API calls.

<Canvas of={SyndicationSearchWidgetStories.StaticMockup} meta={SyndicationSearchWidgetStories} />

## Usage

```jsx
import { SyndicationSearchWidget } from '@undrr/undrr-mangrove';

<SyndicationSearchWidget
  config={{
    searchEndpoint: 'https://www.undrr.org/search-endpoint',
    resultsPerPage: 5,
    defaultFilters: [{ key: '_language', value: 'en' }],
  }}
/>
```

### Integration with Drupal

The component is loaded automatically on pages containing a container with `data-undrr-search-widget`:

```html
<div
  data-undrr-search-widget
  data-search-endpoint="https://www.undrr.org/search-endpoint"
  data-results-per-page="50"
  data-default-query=""
  data-default-sort="relevance"
  data-show-facets="true"
  data-default-filters='[{"key":"_language","value":"en"}]'
>
  <!-- React SearchWidget mounts here -->
</div>
```

## Props

### Config object

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `searchEndpoint` | string | `/search-endpoint` | API endpoint for Elasticsearch queries |
| `resultsPerPage` | number | 50 | Number of results to display per page |
| `debounceDelay` | number | 500 | Milliseconds to wait before executing search |
| `minSearchLength` | number | 3 | Minimum characters required to trigger search |
| `defaultQuery` | string | `''` | Initial search query |
| `defaultSort` | string | `'relevance'` | Initial sort order (`relevance`, `newest`, `oldest`) |
| `defaultFilters` | array | `[]` | Default facet filters (array of `{key, value}`) |
| `showSearchBox` | boolean | `true` | Show the search input |
| `showResultsCount` | boolean | `true` | Show "Showing X of Y results" |
| `showSearchTimer` | boolean | `true` | Show search execution time |
| `showFacets` | boolean | `true` | Show the facets sidebar |
| `showActiveFilters` | boolean | `true` | Show active filter chips |
| `showSearchMetrics` | boolean | `false` | Show debug metrics (score, etc.) |
| `enableHashSync` | string/boolean | `'auto'` | Sync search state to URL hash |
| `visibleFilters` | object | `null` | Control which filters are visible |
| `allowedTypes` | object | `null` | Restrict content types shown |
| `customFilters` | array | `[]` | Additional Elasticsearch filters |
| `customFacets` | array | `[]` | Custom facet definitions |
| `displayMode` | string | `'list'` | Result rendering style: `'list'`, `'card'`, or `'card-book'` |
| `gridColumns` | number | `null` | Grid columns for card modes (2-6). `null` = use `resultsPerPage` |
| `visibleTeaserFields` | object | `null` | Toggle teaser field visibility (`{image: false, ...}`). `null` = all visible |
| `requireImage` | boolean | `false` | Only return results that have an image (`has_image:true` ES filter) |
| `interestingnessTiers` | array | `[]` | Restrict by editorial weight tiers (e.g., `['promoted', 'announced']`) |
| `longevityTiers` | array | `[]` | Restrict by content freshness tiers (e.g., `['year', 'longtime', 'always']`) |

## Accessibility

- All interactive elements have proper ARIA labels
- Keyboard navigation with Tab, Enter, and Escape
- Screen reader announcements for result counts and filter changes
- Focus management on filter removal

## Server-rendered / CDN usage

SyndicationSearchWidget supports [layered hydration](https://github.com/unisdr/undrr-mangrove/blob/main/docs/HYDRATION.md). Use data attributes on a container element to configure the widget from server-rendered HTML — all attributes are optional:

```html
<div data-mg-search-widget
  data-search-endpoint="/search-endpoint"
  data-results-per-page="50"
  data-default-query=""
  data-default-sort="relevance"
  data-display-mode="list"
  data-show-facets="true"
  data-show-search-box="true"
  data-show-results-count="true"
  data-show-active-filters="true"
  data-show-pager="true"
  data-enable-hash-sync="true"
  data-default-filters='[{"key":"_language","value":"en"}]'>
</div>
```

| Attribute | Default | Description |
|-----------|---------|-------------|
| `data-search-endpoint` | — | Elasticsearch proxy URL |
| `data-results-per-page` | — | Number of results per page |
| `data-debounce-delay` | — | Milliseconds before executing search |
| `data-min-search-length` | — | Minimum characters to trigger search |
| `data-default-query` | — | Initial search query |
| `data-default-sort` | — | Initial sort order |
| `data-display-mode` | — | `'list'`, `'card'`, or `'card-book'` |
| `data-grid-columns` | — | Grid columns for card modes (2-6) |
| `data-query-append` | — | Query string to append to all searches |
| `data-show-search-box` | — | Show search input |
| `data-show-results-count` | — | Show result count |
| `data-show-facets` | — | Show facets sidebar |
| `data-show-active-filters` | — | Show active filter chips |
| `data-show-pager` | — | Show pagination |
| `data-show-search-metrics` | — | Show debug metrics |
| `data-show-search-timer` | — | Show search execution time |
| `data-enable-hash-sync` | — | Sync search state to URL hash |
| `data-default-filters` | — | JSON array of `{key, value}` filter objects |
| `data-allowed-types` | — | JSON object restricting content type options |
| `data-require-image` | — | Only return results with images (`true`/`false`) |
| `data-interestingness-tiers` | — | JSON array of tier names (e.g., `'["promoted","announced"]'`) |
| `data-longevity-tiers` | — | JSON array of tier names (e.g., `'["year","longtime","always"]'`) |
| `data-custom-filters` | — | JSON array of custom filter strings (e.g., `'["type:news"]'`) |
| `data-custom-facets` | — | JSON array of custom facet objects |
| `data-visible-teaser-fields` | — | JSON array of teaser field names to show |

All attributes are optional. Only attributes present on the container are included in the config; the component uses its own defaults for anything missing.

```js
import createHydrator from "https://assets.undrr.org/static/mangrove/1.5.0/components/hydrate.js";
import SyndicationSearchWidget, { fromElement } from "https://assets.undrr.org/static/mangrove/1.5.0/components/SyndicationSearchWidget.js";
createHydrator({ selector: "[data-mg-search-widget]", component: SyndicationSearchWidget, fromElement });
```

See the [hydration guide](https://github.com/unisdr/undrr-mangrove/blob/main/docs/HYDRATION.md) for the full API and integration details.

> **Integration note:** This is a complex-tier component. `fromElement` extracts only the attributes present on the container — Drupal modules and other consumer wrappers can merge additional config (`customFacets`, `customFilters`, etc.) that is too complex to express as data attributes.

## Architecture

> **Implementation note (for contributors):** Uses React 19 concurrent features: `useDeferredValue` keeps input responsive while results load; `useTransition` marks filter changes as non-urgent; `Suspense` provides loading skeletons; Context + `useReducer` manages search state without external dependencies.

### Component structure

```
SyndicationSearchWidget
├── SearchProvider (context)
│   ├── SearchForm (input + button)
│   ├── ActiveFilters (filter chips)
│   ├── SearchResults (with Suspense)
│   │   └── ResultItem (×n)
│   ├── FacetsSidebar (desktop)
│   │   ├── SortOptions (relevance/newest/oldest)
│   │   ├── FacetSelect (×n, from Elasticsearch aggregations)
│   │   │   └── SelectDropdown (custom Chosen-like component)
│   │   └── CustomFacetSelect (×n, editor-defined)
│   └── MobileFilterDrawer (mobile, <800px)
│       └── FacetsSidebar (reused)
```

### Custom select dropdown

The facet dropdowns use a custom `SelectDropdown` component instead of Chosen.js, with the same core functionality and no external dependencies:

| Feature | Description |
|---------|-------------|
| **Searchable** | Filter input shown when options > 8 |
| **Multi-select** | Checkbox indicators, multiple selections |
| **Single-select** | Radio indicators, one selection |
| **Keyboard navigation** | Arrow keys, Enter, Escape |
| **Touch-friendly** | 44px trigger height, 40px option height |
| **Accessible** | ARIA listbox pattern, screen reader support |

This means the component works in any environment (Drupal, Storybook, standalone) without requiring Chosen.js to be loaded.

### Mobile filter drawer

On screens ≤800px, the sidebar is replaced with a left-slide drawer:

- Amazon-style slide-in from the left edge
- Click backdrop to close (right side)
- Sticky header and footer with "View results" button
- Focus trapping for accessibility

UX patterns informed by [Pencil & Paper's mobile filter analysis](https://www.pencilandpaper.io/articles/ux-pattern-analysis-mobile-filters).

## Changelog

- **1.2.4** — 2026-04-09 ([web-backlog#919](https://github.com/unisdr/undrr-mangrove/issues/919)): `fromElement` now parses `data-custom-filters`, `data-custom-facets`, and `data-visible-teaser-fields` as JSON, consistent with `defaultFilters` and `allowedTypes`
- **1.2.3** — 2026-03-19 ([web-backlog#2630](https://gitlab.com/undrr/web-backlog/-/work_items/2630)): When both `siteName` and `contentType` are hidden via `visibleTeaserFields`, the `.mg-search__result-badges` wrapper div is no longer injected into the DOM. Previously the empty wrapper was still rendered and hidden by CSS.
- **1.2.2** — 2026-03-19: Fixed search input UI bounce — `useDeferredValue` stale flag was triggering heavy visual feedback (progress bar, button text swap, pulsing overlay, opacity drop) on every keystroke; now only `isLoading` and `isPending` drive visual indicators; increased default `debounceDelay` from 300ms to 500ms to reduce wasted API calls while staying under the 1-second perceived delay threshold; fixed `?text=` and `?query=` URL parameters being ignored due to race condition where `INITIALIZE` action overwrote the query set by `useHashSync`
- **1.2.1** — 2026-03-13: ([web-backlog#2660](https://gitlab.com/undrr/web-backlog/-/issues/2660)) Native `requireImage`, `interestingnessTiers`, `longevityTiers` config options (moved from Drupal wrapper); `TEASER_FIELDS` constant and `allTeaserFieldsVisible()` as single source of truth for teaser field visibility; ES query optimization (skip aggs/highlights/post_filter for syndication embeds); card-aware loading skeletons; SyndicatedCards story pattern
- **1.2.0** — 2026-03-04: Added hydration support with `fromElement` prop extraction and `createHydrator` integration
- **1.1.0** — 2026-02-26:
  - ([web-backlog#1992](https://gitlab.com/undrr/web-backlog/-/issues/1992)) Taxonomy term results — search results now include taxonomy terms (Hazard, Country, Theme) alongside content nodes; `vid:` prefix in type filter for vocabulary filtering; configurable `searchEndpoint` with CustomEndpoint story documenting override patterns
  - ([web-backlog#2636](https://gitlab.com/undrr/web-backlog/-/issues/2636)) Card display modes (`displayMode`: list/card/card-book) with `gridColumns` control; teaser field visibility (`visibleTeaserFields`) with CSS-based hiding for 7 toggleable fields; Drupal image style rewriting for card aspect ratios
- **1.0.0** — 2026-02-13: Pager styles extracted to standalone `Components/Pager` component; internal `Pager.jsx` is now a thin wrapper; no API changes
- **1.0.0-alpha.2** — 2026-01-01: Mobile filter drawer; custom SelectDropdown replacing Chosen.js (searchable, keyboard navigable, touch-friendly); loading state improvements; compact mobile filter button
- **1.0.0-alpha.1** — 2025-12-12: Initial React 19 implementation with faceted filtering, URL hash sync, Elasticsearch teaser support, taxonomy facet labels, custom facets, Google Analytics integration, skeleton loading
