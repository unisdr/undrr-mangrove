import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import * as TypeScriptStories from './TypeScriptExampleComponent.stories';

<Meta of={TypeScriptStories} />

# TypeScript demo component

This component demonstrates use of TypeScript in Storybook components while maintaining compatibility with the exisiting JavaScript codebase.

## Related

- [TypeScript in Storybook](https://storybook.js.org/docs/react/configure/typescript)
- [Official TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [TypeScript React Cheatsheet](https://github.com/typescript-cheatsheets/react)

## Example

<Canvas of={TypeScriptStories.Submit} meta={TypeScriptStories} />

## Working with non-TypeScript code

When integrating with JavaScript components:

```typescript
// Type declarations for existing JS components
declare module 'javascript-component' {
  export interface Props {
    value: string;
    onChange: (value: string) => void;
  }
  export default function Component(props: Props): JSX.Element;
}
```
