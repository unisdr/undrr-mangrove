import { Canvas, Meta, Story, Source} from '@storybook/addon-docs';
import { Grid } from './Grid';

<Meta title="Design decisions/Grid layout" parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true }
    }
  }}/>

# Grid system
A grid is a series of rows where the developer specifies the content. This grid system is based on CSS Grid.

Note this implementation is to be done.

<Canvas>
  <Grid></Grid>;
</Canvas>

### Usage
To come.


### Changelog
1.0 — Released component
