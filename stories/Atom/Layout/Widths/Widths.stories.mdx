import { Meta, Story, Source} from '@storybook/addon-docs';
import { DesignTokenDocBlock } from 'storybook-design-token/dist/doc-blocks';

<Meta title="Design decisions/Widths" parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true }
    }
  }}/>

# Widths

## NOTE:

These are legacy from the UNDP systems and do not appear to be used, they may be deleted or greatly scaled back.

<DesignTokenDocBlock categoryName="Widths" maxHeight={600} viewType="table" />
