import { Canvas, Meta, Story, Source} from '@storybook/blocks';
import { Container } from './Container';
import * as ContainerStories from './Container.stories';

<Meta
  of={ContainerStories}
  title="Design decisions/Container"
  parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true }
    }
  }}
/>

# Container

Use the `Container` component (`.mg-container`) to establish a basic page width that is responsive.

- `.mg-container--slim` variant that limits width to desktop breakpoint without expanding to wide breakpoint
- `.mg-container--spacer` can be used to apply automatic "smart" spacing between direct child elements.

<Canvas>
  <Story name="Default">
    <Container className="mg-container--spacer">
      <h1>Responsive Container</h1>
      <p>This container adjusts its width based on the screen size.</p>
    </Container>
  </Story>
</Canvas>

## Breakpoints

The component uses the following SASS breakpoints:

- **Mobile**: `$mg-breakpoint-mobile`
- **Tablet**: `$mg-breakpoint-tablet`
- **Desktop**: `$mg-breakpoint-desktop`
- **Desktop wide**: `$mg-breakpoint-desktop`

For more information about breakpoints, see the [Breakpoints story](?path=/docs/design-decisions-breakpoint--docs).

## Example

```jsx
<Container>
  <h1>Responsive Container</h1>
  <p>This container adjusts its width based on the screen size.</p>
</Container>
```

## See also

- [`mg-grid`](?path=/docs/design-decisions-grid--docs)
- [`mg-container-full-width`](?path=/docs/components-fullwidth--docs)

## Changelog

- 1.3 - Refines padding on `.mg-container` to not set top/bottom padding.
- 1.2 - Adds `.mg-container--slim` variant that limits width to desktop breakpoint without expanding to wide breakpoint
- 1.1 - Adds `.mg-container--spacer` (`--mg-container-spacer`) for between element spacing.